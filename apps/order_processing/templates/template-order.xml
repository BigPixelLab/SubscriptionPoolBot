<template requires="order">
    <span>
        <span if="order.processing_employee_id"> 🔹 </span>
        <span else=""> 🔸 </span>
        #{order.id} <chat user.py="order.client_id"/> {order.subscription.title}
    </span>
</template>